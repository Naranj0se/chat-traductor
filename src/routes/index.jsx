import { Route, Routes } from 'react-router-dom';

const RouterApp = () => {
    <Routes> 
        <Route path='/' element={} />
        <Route path='/auth' element={}/>
    </Routes>
}

export default RouterApp